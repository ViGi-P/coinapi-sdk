#' CoinAPI Indexes REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Contact: support@apibricks.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title IndexInputData operations
#' @description IndexInputDataApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  V1IndexdefInputDataIndexDefinitionIdAllGet  ####################
#'
#' library(openapi)
#' var_index_definition_id <- "index_definition_id_example" # character | 
#'
#' #Returns all data inputs for a specific index definition
#' api_instance <- IndexInputDataApi$new()
#'
#' # Configure API key authorization: APIKey
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: JWT
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1IndexdefInputDataIndexDefinitionIdAllGet(var_index_definition_iddata_file = "result.txt")
#' result <- api_instance$V1IndexdefInputDataIndexDefinitionIdAllGet(var_index_definition_id)
#' dput(result)
#'
#'
#' ####################  V1IndexdefInputDataIndexDefinitionIdGet  ####################
#'
#' library(openapi)
#' var_index_definition_id <- "index_definition_id_example" # character | 
#' var_time <- "time_example" # character |  (Optional)
#' var_enabled_only <- FALSE # character |  (Optional)
#' var_pending_only <- FALSE # character |  (Optional)
#' var_filter_asset_id <- "filter_asset_id_example" # character |  (Optional)
#' var_with_status_info <- FALSE # character |  (Optional)
#'
#' #Returns data inputs for certain index definition and time
#' api_instance <- IndexInputDataApi$new()
#'
#' # Configure API key authorization: APIKey
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: JWT
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1IndexdefInputDataIndexDefinitionIdGet(var_index_definition_id, time = var_time, enabled_only = var_enabled_only, pending_only = var_pending_only, filter_asset_id = var_filter_asset_id, with_status_info = var_with_status_infodata_file = "result.txt")
#' result <- api_instance$V1IndexdefInputDataIndexDefinitionIdGet(var_index_definition_id, time = var_time, enabled_only = var_enabled_only, pending_only = var_pending_only, filter_asset_id = var_filter_asset_id, with_status_info = var_with_status_info)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
IndexInputDataApi <- R6::R6Class(
  "IndexInputDataApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new IndexInputDataApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Returns all data inputs for a specific index definition
    #'
    #' @param index_definition_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ModelsIndexDefinitionInputData]
    V1IndexdefInputDataIndexDefinitionIdAllGet = function(index_definition_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$V1IndexdefInputDataIndexDefinitionIdAllGetWithHttpInfo(index_definition_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns all data inputs for a specific index definition
    #'
    #' @param index_definition_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ModelsIndexDefinitionInputData]) with additional information such as HTTP status code, headers
    V1IndexdefInputDataIndexDefinitionIdAllGetWithHttpInfo = function(index_definition_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`index_definition_id`)) {
        stop("Missing required parameter `index_definition_id`.")
      }

      if (!missing(`index_definition_id`) && is.null(`index_definition_id`)) {
        stop("Invalid value for `index_definition_id` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdAllGet, `index_definition_id` is not nullable")
      }

      local_var_url_path <- "/v1/indexdef/input-data/{index_definition_id}/all"
      if (!missing(`index_definition_id`)) {
        local_var_url_path <- gsub("\\{index_definition_id\\}", URLencode(as.character(`index_definition_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json", "application/x-msgpack")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ModelsIndexDefinitionInputData]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns data inputs for certain index definition and time
    #'
    #' @param index_definition_id 
    #' @param time (optional) No description
    #' @param enabled_only (optional) No description (default value: FALSE)
    #' @param pending_only (optional) No description (default value: FALSE)
    #' @param filter_asset_id (optional) No description
    #' @param with_status_info (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ModelsIndexDefinitionSnapshotEntry]
    V1IndexdefInputDataIndexDefinitionIdGet = function(index_definition_id, time = NULL, enabled_only = FALSE, pending_only = FALSE, filter_asset_id = NULL, with_status_info = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$V1IndexdefInputDataIndexDefinitionIdGetWithHttpInfo(index_definition_id, time, enabled_only, pending_only, filter_asset_id, with_status_info, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns data inputs for certain index definition and time
    #'
    #' @param index_definition_id 
    #' @param time (optional) No description
    #' @param enabled_only (optional) No description (default value: FALSE)
    #' @param pending_only (optional) No description (default value: FALSE)
    #' @param filter_asset_id (optional) No description
    #' @param with_status_info (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ModelsIndexDefinitionSnapshotEntry]) with additional information such as HTTP status code, headers
    V1IndexdefInputDataIndexDefinitionIdGetWithHttpInfo = function(index_definition_id, time = NULL, enabled_only = FALSE, pending_only = FALSE, filter_asset_id = NULL, with_status_info = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`index_definition_id`)) {
        stop("Missing required parameter `index_definition_id`.")
      }

      if (!missing(`index_definition_id`) && is.null(`index_definition_id`)) {
        stop("Invalid value for `index_definition_id` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdGet, `index_definition_id` is not nullable")
      }

      if (!missing(`time`) && is.null(`time`)) {
        stop("Invalid value for `time` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdGet, `time` is not nullable")
      }

      if (!missing(`enabled_only`) && is.null(`enabled_only`)) {
        stop("Invalid value for `enabled_only` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdGet, `enabled_only` is not nullable")
      }

      if (!missing(`pending_only`) && is.null(`pending_only`)) {
        stop("Invalid value for `pending_only` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdGet, `pending_only` is not nullable")
      }

      if (!missing(`filter_asset_id`) && is.null(`filter_asset_id`)) {
        stop("Invalid value for `filter_asset_id` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdGet, `filter_asset_id` is not nullable")
      }

      if (!missing(`with_status_info`) && is.null(`with_status_info`)) {
        stop("Invalid value for `with_status_info` when calling IndexInputDataApi$V1IndexdefInputDataIndexDefinitionIdGet, `with_status_info` is not nullable")
      }

      query_params[["time"]] <- `time`

      query_params[["enabled_only"]] <- `enabled_only`

      query_params[["pending_only"]] <- `pending_only`

      query_params[["filter_asset_id"]] <- `filter_asset_id`

      query_params[["with_status_info"]] <- `with_status_info`

      local_var_url_path <- "/v1/indexdef/input-data/{index_definition_id}"
      if (!missing(`index_definition_id`)) {
        local_var_url_path <- gsub("\\{index_definition_id\\}", URLencode(as.character(`index_definition_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json", "application/x-msgpack")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ModelsIndexDefinitionSnapshotEntry]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
