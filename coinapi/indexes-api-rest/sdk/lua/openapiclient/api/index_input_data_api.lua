--[[
  CoinAPI Indexes REST API

  No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

  The version of the OpenAPI document: v1
  Contact: support@apibricks.io
  Generated by: https://openapi-generator.tech
]]

--package openapiclient

local http_request = require "http.request"
local http_util = require "http.util"
local dkjson = require "dkjson"
local basexx = require "basexx"

-- model import
local openapiclient_models_index_definition_input_data = require "openapiclient.model.models_index_definition_input_data"
local openapiclient_models_index_definition_snapshot_entry = require "openapiclient.model.models_index_definition_snapshot_entry"

local index_input_data_api = {}
local index_input_data_api_mt = {
	__name = "index_input_data_api";
	__index = index_input_data_api;
}

local function new_index_input_data_api(authority, basePath, schemes)
	local schemes_map = {}
	for _,v in ipairs(schemes) do
		schemes_map[v] = v
	end
	local default_scheme = schemes_map.https or schemes_map.http
	local host, port = http_util.split_authority(authority, default_scheme)
	return setmetatable({
		host = host;
		port = port;
		basePath = basePath or "https://rest-api.indexes.coinapi.io";
		schemes = schemes_map;
		default_scheme = default_scheme;
		http_username = nil;
		http_password = nil;
		api_key = {};
		access_token = nil;
	}, index_input_data_api_mt)
end

function index_input_data_api:v1_indexdef_input_data_index_definition_id_all_get(index_definition_id)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/indexdef/input-data/%s/all",
			self.basePath, index_definition_id);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")

	-- api key in headers 'Authorization'
	if self.api_key['Authorization'] then
		req.headers:upsert("APIKey", self.api_key['Authorization'])
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_models_index_definition_input_data.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function index_input_data_api:v1_indexdef_input_data_index_definition_id_get(index_definition_id, time, enabled_only, pending_only, filter_asset_id, with_status_info)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/indexdef/input-data/%s?time=%s&enabled_only=%s&pending_only=%s&filter_asset_id=%s&with_status_info=%s",
			self.basePath, index_definition_id, http_util.encodeURIComponent(time), http_util.encodeURIComponent(enabled_only), http_util.encodeURIComponent(pending_only), http_util.encodeURIComponent(filter_asset_id), http_util.encodeURIComponent(with_status_info));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")

	-- api key in headers 'Authorization'
	if self.api_key['Authorization'] then
		req.headers:upsert("APIKey", self.api_key['Authorization'])
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_models_index_definition_snapshot_entry.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

return {
	new = new_index_input_data_api;
}
